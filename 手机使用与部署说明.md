# 深宫浮生录 · 手机端使用与部署说明

纯静态 HTML 游戏（`index.html` + `street.html` + 图片资源），无需后端，可部署到任意静态托管。

## ✅ 线上已部署（GitHub Pages）

手机直接访问（无需局域网）：

```
https://dnsjohfn.github.io/shengong-game-v2/
```

- 仓库：`dnsjohfn/shengong-game-v2`（分支 `main`，根目录部署）
- 已启用 PWA：手机浏览器打开后，可在菜单选择 **“添加到主屏幕”**，安装后全屏横屏运行、支持离线缓存
- 更新方式：本地修改后 `git add .` → `git commit` → `git push v2 main`，GitHub Pages 自动重新构建（约 1~2 分钟）

## 一、手机直接游玩（局域网）

1. 在项目目录启动本地服务器（已内置）：

   ```powershell
   cd c:\Users\33437\Desktop\hyml
   python -m http.server 8000 --bind 0.0.0.0
   ```

2. 手机连接**同一 Wi-Fi**，浏览器打开：

   ```
   http://192.168.10.108:8000/index.html
   ```

   （IP 以本机实际 WLAN 地址为准，可用 `ipconfig` 查看）

3. 若手机打不开，多为 Windows 防火墙拦截：首次运行允许“专用网络”访问，或临时关闭防火墙。

## 二、横屏适配说明

- **竖屏打开**：显示横屏引导页，点击 **“进入横屏模式”** 会请求全屏并尝试锁定横屏。
  - Android Chrome：全屏后直接锁定横屏（真横屏）。
  - iOS Safari：不支持锁定横屏，自动降级为“旋转画面铺满 + 顶部小提示”。
- **横屏打开**：直接正常游玩，无引导。
- 支持 PWA：安装到主屏幕后（见下）以 `fullscreen` + `landscape` 运行。

## 三、PWA 安装（需 HTTPS）

> 浏览器只允许在 **HTTPS 或 localhost** 下安装 PWA、注册 Service Worker；局域网 `http://` 无法安装。

### 方式 A：GitHub Pages（推荐，免费）

1. 新建 GitHub 仓库并上传本项目全部文件（保持目录结构）。
2. 仓库 Settings → Pages → 选择分支 `main`，根目录 `/` 部署。
3. 访问 `https://<你的用户名>.github.io/<仓库名>/index.html`。
4. Android Chrome 打开后，浏览器菜单会提示“添加到主屏幕”；
   iOS Safari 用“分享 → 添加到主屏幕”，安装后即为全屏横屏应用。

### 方式 B：Vercel / Netlify

- 直接拖拽项目文件夹到 [vercel.com](https://vercel.com) / [netlify.com](https://netlify.com)，零配置即可上线 HTTPS。

## 四、离线缓存

`sw.js` 已实现 Service Worker 动态缓存：首次访问后静态资源离线可用（同样需要 HTTPS 环境才会生效）。

## 五、项目文件

| 文件 | 说明 |
| ---- | ---- |
| `index.html` | 主游戏（封面/主界面/状态/存档/本季安排/背包/日记/交谈） |
| `street.html` | 上街（场景切换与行动） |
| `manifest.webmanifest` | PWA 清单（全屏横屏 + 图标） |
| `sw.js` | Service Worker 离线缓存 |
| `icons/` | 应用图标（512/192/180） |
